"""Higher-order context filters (time windows, IPDA state, IRL/ERL).

Skeleton-only for now — filters will be plugged in front of the detector
in a follow-up so that signals are only emitted when both price AND time
align (per CRT doctrine §6).
"""
